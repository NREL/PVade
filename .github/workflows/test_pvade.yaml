name: test_pvade

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
  
jobs:
  build:
    name: test_pvade - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest"]
        python-version: ["3.10"]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v2
        with:
          python-version: "3.10"
      - uses: mamba-org/provision-with-micromamba@main
        with:
          channel-priority: disabled
          environment-file: environment.yaml
      # - name: Bash
      #   shell: bash -l {0}
      #   run: pyright .
      # - name: Run PyLint
      #   shell: bash -l {0}
      #   run: pylint main.py pkg/
      - name: Run PyTest
        shell: bash -l {0}
        run: pytest .
  conda-install:
    name: test_pvade - ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest"]
    steps:
      # - name: Update conda
      #   run: |
      #     conda update conda
      - name: Install DOLFINx (py3-9)
        run: conda create -n env3-9 -c conda-forge python=3.9 fenics-dolfinx mpich python-gmsh
      - name: Test (py3-9)
        run: |
          conda run -n env3-9 python -c "import dolfinx; import gmsh;"
    # steps:
    #   - uses: actions/checkout@v2

    #   - name: Install Conda environment with Micromamba
    #     uses: mamba-org/provision-with-micromamba@main
    #     with:
    #       environment-file: myenv.yaml
    #       environment-name: myenvname
    #       extra-specs: |
    #         python=3.7
    #         pytest=${{ matrix.pytest }}
